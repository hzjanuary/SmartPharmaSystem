// const cors = require('cors');

// // Cấu hình CORS chuẩn (Chỉ có 1 đoạn này thôi nhé)
// app.use(cors({
//     origin: 'http://localhost:5173', // Địa chỉ Frontend React của bạn
//     credentials: true                // Cho phép nhận Cookie/Session
// }));